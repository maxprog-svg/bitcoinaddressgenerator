The Bitcoin Address Generator is a program designed to generate unique Bitcoin addresses and their corresponding private keys. This program uses the ecdsa library to implement the ECDSA algorithm .

pip install ecdsa

python3 btcgen.py
